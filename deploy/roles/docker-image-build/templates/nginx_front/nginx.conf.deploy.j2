worker_processes 4;
worker_rlimit_nofile 32768;

events {
    worker_connections 4096;
}

http {
    upstream tornado_app {
        server {{ tornado_host }}:{{ tornado_port }};
    }

    server {
        listen 80;
        location / {
            proxy_pass http://tornado_app;
        }
    }
}